var fnguard = require('./fnguard');

// utility;

// if called from command line...
if (require.main === module) {
  //options = UserOptions.getNew(argv);
  //scrounge.go(options);  
} 


module.exports = fnguard;
